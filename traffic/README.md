** Start 
In the first trial, one Convolution and Pooling layer with convolution size (3, 3) 32 filters 
and Pooling size (2, 2) was used. One (1) hidden layer of 128 units with dropout. The model was trained with loss of 3.5033 and 0.0563 accuracy

In the second trial, one Convolution and Pooling layer with convolution size (3, 3) 32 filters 
and Pooling size (2, 2) was used. One (1) hidden layer of 1000 units with dropout. The model was trained with loss of 0.6103 and 0.9137 accuracy

In the third trial, two Convolution and Pooling layers with convolution size (3, 3) 32 filters 
and Pooling size (2, 2) was used. Two (2) hidden layers of 700 units each with dropout. The model was trained with loss of 0.1242 and 0.9521 accuracy

In the fourth trial, three Convolution and Pooling layers with convolution size (3, 3) 32, 64 and 128 filters and Pooling size (2, 2) was used. One (1) hidden layers of 256 units with dropout. The model was trained with loss of 0.0913 and 0.9794 accuracy